// placeholder: 旧的临时 config 验证脚本。
// 真正的覆盖顺序测试在 internal/config/config_test.go
// (5 个 test 覆盖 default/.env/env/$HOME/.collect-ai/godotenv 行为)
package main

func main() {
	// 真正跑测试用: go test ./internal/config/... -v
}
